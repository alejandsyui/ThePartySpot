# Live Session Tracker Spec

States: pending → confirmed → preparing → live → wrapping_up → completed

Tracker behavior:
- Show step markers and timestamps
- Compute ETA if durations configured
- Animate progress bar on state changes
